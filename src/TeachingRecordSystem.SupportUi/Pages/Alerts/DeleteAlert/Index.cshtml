@page "/alerts/{alertId}/delete"
@model TeachingRecordSystem.SupportUi.Pages.Alerts.DeleteAlert.IndexModel
@{
    ViewBag.Title = "Why are you deleting this alert?";
}

@section BeforeContent {
    @if (Model.BackLink is not null)
    {
        <govuk-back-link href="@Model.BackLink">Back</govuk-back-link>
    }
}

<div class="govuk-grid-row">
    <div class="govuk-grid-column-two-thirds">
        <form method="post">
            <span class="govuk-caption-l">Delete alert - @Model.PersonName</span>
            <h1 class="govuk-heading-l" data-testid="title">@ViewBag.Title</h1>
            <p class="govuk-body"><strong>Alert type:</strong> @Model.AlertTypeName</p>

            <govuk-radios for="DeleteReason">
                <radios-item value="@DeleteAlertReasonOption.AddedInError">
                    @DeleteAlertReasonOption.AddedInError.GetDisplayName()
                </radios-item>
                <radios-item value="@DeleteAlertReasonOption.AnotherReason">
                    @DeleteAlertReasonOption.AnotherReason.GetDisplayName()
                    <conditional data-testid="delete-reason-detail">
                        <govuk-input data-testid="reason-detail" for="DeleteReasonDetail" input-class="govuk-input--width-20" spellcheck="false">
                            <label>Enter a reason</label>
                        </govuk-input>
                    </conditional>
                </radios-item>
            </govuk-radios>

            <govuk-radios for="ProvideAdditionalInformation">
                <legend class="govuk-fieldset__legend--m">
                    Do you want to provide more information?
                </legend>
                <radios-item value="@true">
                    Yes
                    <conditional>
                        <govuk-character-count for="AdditionalInformation" max-length="UiDefaults.ReasonDetailsMaxCharacterCount" rows="UiDefaults.ReasonDetailsRows">
                            <label>Enter details</label>
                        </govuk-character-count>
                    </conditional>
                </radios-item>
                <radios-item value="@false">
                    No
                </radios-item>
            </govuk-radios>

            @Html.EditorFor(m => m.Evidence)

            <div class="govuk-button-group">
                <govuk-button type="submit">Continue</govuk-button>
                <govuk-button name="Cancel" value="true" class="govuk-button--secondary" type="submit">Cancel and return to record</govuk-button>
            </div>
        </form>
    </div>
</div>

