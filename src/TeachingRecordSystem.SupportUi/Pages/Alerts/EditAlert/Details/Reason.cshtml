@page "/alerts/{alertId}/details/reason"
@model TeachingRecordSystem.SupportUi.Pages.Alerts.EditAlert.Details.ReasonModel
@{
    ViewBag.Title = "Why are you changing the alert details?";
}

@section BeforeContent {
    @if (Model.BackLink is not null)
    {
        <govuk-back-link href="@Model.BackLink" />
    }
}

<div class="govuk-grid-row">
    <div class="govuk-grid-column-two-thirds">
        <form method="post">
            <span class="govuk-caption-l">Edit alert - @Model.PersonName</span>
            <h1 class="govuk-heading-l" data-testid="title">@ViewBag.Title</h1>

            <govuk-radios for="ChangeReason" data-testid="change-reason-options">
                <radios-item value="@AlertChangeDetailsReasonOption.IncorrectDetails">
                    @AlertChangeDetailsReasonOption.IncorrectDetails.GetDisplayName()
                </radios-item>
                <radios-item value="@AlertChangeDetailsReasonOption.ChangeOfDetails">
                    @AlertChangeDetailsReasonOption.ChangeOfDetails.GetDisplayName()
                </radios-item>
                <radios-item value="@AlertChangeDetailsReasonOption.AnotherReason">
                    @AlertChangeDetailsReasonOption.AnotherReason.GetDisplayName()
                    <conditional data-testid="add-reason-detail">
                        <govuk-input data-testid="reason-detail" for="ChangeReasonDetail" input-class="govuk-input--width-20" spellcheck="false">
                            <label>Enter a reason</label>
                        </govuk-input>
                    </conditional>
                </radios-item>
            </govuk-radios>

            <govuk-radios for="ProvideAdditionalInformation" data-testid="provide-additional-information">
                <legend>
                    <h2 class="govuk-fieldset__legend--m">Do you want to provide more information?</h2>
                </legend>
                <radios-item value="@true">
                    Yes
                    <conditional>
                        <govuk-character-count for="AdditionalInformation" max-length="UiDefaults.ReasonDetailsMaxCharacterCount" rows="UiDefaults.ReasonDetailsRows">
                            <label>Enter details</label>
                        </govuk-character-count>
                    </conditional>
                </radios-item>
                <radios-item value="@false">
                    No
                </radios-item>
            </govuk-radios>

            @Html.EditorFor(m => m.Evidence)

            <div class="govuk-button-group">
                <govuk-button type="submit">Continue</govuk-button>
                <govuk-button name="Cancel" value="true" class="govuk-button--secondary" type="submit">Cancel and return to record</govuk-button>
            </div>
        </form>
    </div>
</div>
