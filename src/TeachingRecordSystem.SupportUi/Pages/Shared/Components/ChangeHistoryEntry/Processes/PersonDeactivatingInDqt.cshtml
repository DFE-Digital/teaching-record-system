@model ChangeHistoryEntryViewModel
@{
    var deactivatedEvent = Model.GetEvent<PersonDeactivatedEvent>();
    var personInfo = Model.Context.GetPersonInfo(deactivatedEvent.PersonId);
    var name = string.JoinNonEmpty(' ', personInfo.FirstName, personInfo.LastName);
}

<timeline-entry title="Record deactivated in DQT" by="@Model.UserName" timestamp="@Model.Timestamp" data-process-id="@Model.ProcessId">
    <p class="govuk-body">Record deactivated for @name</p>
</timeline-entry>
