@page "/support-tasks/change-requests/{supportTaskReference}/reject"
@using TeachingRecordSystem.Core.Models.SupportTasks;
@model TeachingRecordSystem.SupportUi.Pages.SupportTasks.ChangeRequests.EditChangeRequest.RejectModel
@{
    ViewBag.Title = "Reject the change";
}

@section BeforeContent {
    <govuk-back-link href="@LinkGenerator.SupportTasks.ChangeRequests.EditChangeRequest.Index(Model.SupportTaskReference)" />
}

<span class="govuk-caption-l">Record management</span>
<h1 class="govuk-heading-l">@(Model.ChangeType == SupportTaskType.ChangeNameRequest ? "Name change request" : "Date of birth change request")</h1>

<div class="govuk-grid-row">
    <div class="govuk-grid-column-two-thirds">
        <form method="post">
            <govuk-radios for="RejectionReasonChoice">
                <govuk-radios-fieldset>
                    <govuk-radios-fieldset-legend>
                        <h3 class="govuk-heading-m">Reason for rejecting</h3>
                    </govuk-radios-fieldset-legend>
                    <govuk-radios-item value="@ChangeRequestRejectReason.RequestAndProofDontMatch">
                        @ChangeRequestRejectReason.RequestAndProofDontMatch.GetDisplayName()
                    </govuk-radios-item>
                    <govuk-radios-item value="@ChangeRequestRejectReason.WrongTypeOfDocument">
                        @ChangeRequestRejectReason.WrongTypeOfDocument.GetDisplayName()
                    </govuk-radios-item>
                    <govuk-radios-item value="@ChangeRequestRejectReason.ImageQuality">
                        @ChangeRequestRejectReason.ImageQuality.GetDisplayName()
                    </govuk-radios-item>
                    <govuk-radios-item value="@ChangeRequestRejectReason.ChangeNoLongerRequired">
                        @ChangeRequestRejectReason.ChangeNoLongerRequired.GetDisplayName()
                    </govuk-radios-item>
                </govuk-radios-fieldset>
            </govuk-radios>

            <govuk-button type="submit">Reject</govuk-button>
        </form>
    </div>
</div>
