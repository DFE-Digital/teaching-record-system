pre-commit:
  commands:
    dotnet-format:
      glob: "QualifiedTeachersApi/**/*.cs"
      run: dotnet format QualifiedTeachersApi/QualifiedTeachersApi.sln --verify-no-changes --include {staged_files}
